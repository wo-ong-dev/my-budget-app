name: Build and Deploy (EC2 Nginx)

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
        env:
          # Set if you want to override at build-time. Optional because code also uses absolute "/api" paths.
          VITE_API_BASE_URL: ${{ secrets.VITE_API_BASE_URL }}

      - name: Add build timestamp
        run: |
          BUILD_TIME=$(date -u +"%Y-%m-%d %H:%M:%S UTC")
          sed -i "s|</head>|  <!-- Build: $BUILD_TIME -->\n  </head>|" dist/index.html
          echo "Added build timestamp: $BUILD_TIME"
          head -10 dist/index.html

      - name: Add SSH key
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.KEY }}

      - name: Deploy to EC2
        run: |
          set -e
          set -x  # Enable debug mode

          # Use secrets directly to avoid env variable conflicts
          SSH_HOST="${{ secrets.HOST }}"
          SSH_USER="${{ secrets.USER }}"
          SSH_PORT="${{ secrets.PORT }}"
          TARGET_DIR="${{ secrets.APP_DIR }}"

          # Remove any whitespace, newlines, carriage returns from secrets
          SSH_HOST=$(echo "$SSH_HOST" | tr -d '[:space:]')
          SSH_USER=$(echo "$SSH_USER" | tr -d '[:space:]')
          SSH_PORT=$(echo "$SSH_PORT" | tr -d '[:space:]')
          TARGET_DIR=$(echo "$TARGET_DIR" | tr -d '[:space:]')

          # Remove common prefixes if accidentally included (e.g., "SSH_HOST:13.125.205.126" -> "13.125.205.126")
          SSH_HOST="${SSH_HOST#*:}"
          SSH_USER="${SSH_USER#*:}"
          SSH_PORT="${SSH_PORT#*:}"
          TARGET_DIR="${TARGET_DIR#*:}"

          # Validate required secrets
          if [ -z "$SSH_HOST" ]; then
            echo "❌ ERROR: HOST secret is not set!"
            exit 1
          fi

          # Set defaults
          SSH_PORT=${SSH_PORT:-22}
          SSH_USER=${SSH_USER:-ec2-user}
          TARGET_DIR=${TARGET_DIR:-/var/www/html}

          echo "======================================"
          echo "🚀 Starting Deployment"
          echo "======================================"
          echo "📡 Host: ${SSH_HOST}"
          echo "👤 User: ${SSH_USER}"
          echo "🔌 Port: ${SSH_PORT}"
          echo "📂 Target: ${TARGET_DIR}"
          echo "======================================"

          # Check SSH key is loaded
          echo "🔑 Checking SSH agent..."
          ssh-add -l || echo "⚠️ No SSH keys loaded"

          # Test SSH connection with verbose output
          echo "🔍 Testing SSH connection..."
          if ! ssh -v -p ${SSH_PORT} -o StrictHostKeyChecking=no -o ConnectTimeout=10 ${SSH_USER}@${SSH_HOST} "echo 'Connection OK'" 2>&1; then
            echo "❌ SSH connection failed!"
            echo "Debug: Trying to connect to ${SSH_USER}@${SSH_HOST}:${SSH_PORT}"
            exit 1
          fi
          echo "✅ SSH connection successful"

          # Create tar archive for faster transfer
          echo "📦 Creating deployment archive..."
          tar -czf deploy.tar.gz -C dist .
          ls -lh deploy.tar.gz

          # Upload archive to EC2
          echo "📤 Uploading files to EC2..."
          scp -P ${SSH_PORT} -o StrictHostKeyChecking=no deploy.tar.gz ${SSH_USER}@${SSH_HOST}:/tmp/deploy.tar.gz
          echo "✅ Upload complete"

          # Deploy on EC2
          echo "🚀 Deploying on EC2..."
          ssh -p ${SSH_PORT} -o StrictHostKeyChecking=no ${SSH_USER}@${SSH_HOST} bash << EOF
            set -e
            set -x

            echo "📂 Extracting files..."
            rm -rf /tmp/my-budget-app
            mkdir -p /tmp/my-budget-app
            tar -xzf /tmp/deploy.tar.gz -C /tmp/my-budget-app/
            ls -la /tmp/my-budget-app/

            echo "📁 Preparing target directory..."
            sudo mkdir -p ${TARGET_DIR}

            echo "🗑️  Removing old files..."
            sudo rm -rf ${TARGET_DIR}/*

            echo "📋 Copying new files..."
            sudo cp -r /tmp/my-budget-app/* ${TARGET_DIR}/

            echo "👤 Setting permissions..."
            sudo chown -R nginx:nginx ${TARGET_DIR} 2>/dev/null || sudo chown -R www-data:www-data ${TARGET_DIR} 2>/dev/null || true

            echo "✅ Files deployed successfully"
            echo "📋 Deployed files:"
            ls -lh ${TARGET_DIR}/
            echo "📦 Assets folder:"
            ls -lh ${TARGET_DIR}/assets/
            echo "📄 index.html content check:"
            head -20 ${TARGET_DIR}/index.html

            echo "🔧 Fixing Nginx configuration conflicts..."
            # Remove old conflicting configuration
            if [ -f /etc/nginx/conf.d/budget-app.conf ]; then
              sudo rm -f /etc/nginx/conf.d/budget-app.conf
              echo "✅ Removed old budget-app.conf"
            fi

            echo "🔄 Stopping Nginx, clearing cache, and restarting..."
            # Stop Nginx first to release file handles
            if command -v systemctl >/dev/null 2>&1; then
              sudo systemctl stop nginx || true
              echo "✅ Nginx stopped"
            fi

            # Clear Nginx cache (common cache locations)
            sudo rm -rf /var/cache/nginx/* 2>/dev/null || true
            sudo rm -rf /var/lib/nginx/cache/* 2>/dev/null || true
            sudo rm -rf /var/run/nginx-cache/* 2>/dev/null || true
            echo "✅ Nginx cache cleared"

            # Full restart to ensure fresh file loading
            if command -v systemctl >/dev/null 2>&1; then
              sudo systemctl start nginx || true
              sudo systemctl status nginx --no-pager || true
              echo "✅ Nginx restarted"
            fi

            echo "🔍 Verifying Nginx is serving correct files..."
            echo "Nginx configuration root directories:"
            sudo grep -r "root " /etc/nginx/ 2>/dev/null | grep -v "#" || true
            echo ""
            echo "Test fetching from localhost:"
            curl -sI http://localhost/ | head -10 || true
            echo ""
            curl -s http://localhost/ | head -20 || true

            echo "🧹 Cleaning up..."
            rm -f /tmp/deploy.tar.gz
            rm -rf /tmp/my-budget-app

            echo "🎉 Deployment completed successfully!"
          EOF

          echo "======================================"
          echo "✅ Deployment Finished"
          echo "======================================"

 
